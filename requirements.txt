# Synthetic Health — SynthFHIR
# Install:  pip install -r requirements.txt
#
# PyTorch note:
#   Colab (T4 GPU): PyTorch with CUDA is pre-installed — pip will skip reinstall.
#   Windows (CPU):  pip installs the CPU-only build automatically.
#   If you need a specific CUDA build locally, install torch separately first:
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
#   then run this file.

# Core ML
sdv>=1.37.0,<2.0.0
torch>=2.0.0

# Data
pandas>=2.0.0
numpy>=1.24.0
fhir.resources>=7.0.0

# Config
pyyaml>=6.0

# Progress + reporting
tqdm>=4.65.0
tabulate>=0.9.0

# Visualisation (Phase 2)
matplotlib>=3.7.0
plotly>=5.17.0
streamlit>=1.28.0

# Statistics (Phase 5 — utility + privacy evaluation)
scikit-learn>=1.3.0
scipy>=1.10.0
#anonymeter>=1.0.0
